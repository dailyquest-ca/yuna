name: weekly-rank
on:
  schedule:
    - cron: '0 12 * * 6'   # Sat 12:00 UTC — after funnel on 1st Saturdays, per plan
  workflow_dispatch: {}
jobs:
  rank:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - run: pip install -e .
      - run: set -o pipefail; python -m yuna.rank 2>&1 | tee /tmp/job.out
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
      - name: autopsy on failure
        if: failure()
        run: python -m yuna.report_fail weekly-rank /tmp/job.out
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
