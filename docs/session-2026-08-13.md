# Session 2026-08-13 — the audit becomes law, the bars get applied, E1 runs

Picked up from [`handoff-2026-08-13.md`](handoff-2026-08-13.md) on branch
`claude/pickup-previous-session-s3wduf`. The handoff named one blocker (A2's slot ordering —
Zak's decision, untouched here), one standing rule to build (the capture audit), and two open
items (the unidentified conformance failure on A2 runs; the §2.5 bars built but never applied).
This session closed everything except the decision, and ran E1 — the next E-item in the work
order's execution routine, unblocked and decisive.

## 1 · The A2 conformance failure was not where the handoff pointed

The handoff said "narrowed to coverage on two earnings clauses." The stored tables say otherwise:
on run 56, M4 coverage reads 0.982 and the blackout 0.948 — both healthy. The failing clause on
all three A2 runs is **Entry — GTC stop-limit, pivot / pivot+2%**, with `violations` equal to the
run's entire trade count (1,352 / 1,024 / 1,014).

The mechanism: the day loop counted every non-base, non-recovery entry in `reentries`, and the
Entry clause's violation waiver knew only the two non-base doors earlier variants had declared —
X1's `reentry_window` and C1's `screen`. A2's `entry_new_high` door was never taught to the
table, so the arm's own entry rule was reported as a thousand undeclared violations of a §3.2
clause about re-entry.

Fixed in the engine: `new_high` entries are counted at their own door (`conf.new_high_entries`),
the Entry clause declares them (`new_highs`), and the waiver excuses them only when the
hypothesis actually sets `entry_new_high` — an undeclared new-high entry still fails, which is
the guard working. Runs 54–56 keep their recorded `false`; the ledger is history, and all three
were invalid as specified anyway.

## 2 · P1 was only half built, and the A2 runs prove it

Runs 54, 55 and 56 **share param_hash `dd8735ff2ac4dace`** while measuring three different
engines — between them the code changed the exit gating, the hair-trigger, and the candidate
pool, and the params never moved. P1's promise was "two runs are the same experiment iff the
hash matches"; the ledger already holds three counterexamples.

`code_stamp()` now hashes `backtest.py` + `signals.py` into the run's params, where it feeds
`param_digest` through the extras. A code-only change moves the hash. A comment-only change also
moves it, which errs in the safe direction — false-different is a shrug, false-same is how runs
54/55/56 got called one experiment.

## 3 · The capture audit is `src/capture_audit.py`, and every run gets it

The standing rule, built as specified — three parts:

1. **The net** — every push the window offered vs what the arm entered. A push is a close above
   the prior 252 own closes (`new_high_breakout`'s own test) that runs **+50% before any close
   gives the level back**; a breakout that returns to its level first is a failed breakout, in
   neither column. Episodes chain after completion, so a decade-long trend counts once per +50%
   leg rather than once per new-high day — this subsumes the handoff's "name-months" bucketing
   and is recorded as the one deviation from its phrasing. Eligibility at the breakout is
   `rank()`'s own L0 census test (≥210 bars in the last 252 union sessions, raw ≥ $5,
   50-session median dollar volume ≥ $10M): a push the census could never admit is not a
   catchable push. No M2, no MCN, no gate — the net exists to measure the filters too.
2. **The ride** — for each caught push, the positions' realized P&L against holding the first
   entry to the +50% mark. Clipped-after-three-days reads as a small share; entered-and-held
   reads near 1.
3. **The junk** — positions overlapping no push of their name, with their P&L.

Writes `stats.capture_audit` on the run row; crashes record themselves there before re-raising
(Actions logs are unreachable — learnings #13). Wired into `backtest.yml` after the conformance
report, under `always()` — a run that failed conformance needs its capture on the table more,
not less. Unit-tested on hand-built tapes (`tests/test_capture_audit.py`): the chaining, the
failed-breakout rule, the unresolved-race rule, the $5-raw and ADDV floors, and all three parts'
arithmetic.

## 4 · §2.5 has a driver: `src/finding.py`

`bars.py` promised "the driver that reads a run and writes a verdict lives elsewhere" — it now
exists. Per run: both cuts (full window + the Aug-2025 OOS cut, sessions from 2025-08-01);
winner-exclusion jackknife at account level (arithmetic, flagged — it flatters, so only a FAIL
is conclusive); the 63-session circular block bootstrap at seed 0; the deflated Sharpe against
the logged E-series trial count; and the 90/10 chassis counterfactual, computed exactly as the
construction that reproduces the recorded +222.10% on run 53's window (validated to the basis
point before use).

Two interpretations are recorded rather than assumed:

- **Trials (forward-only, per Zak's 2026-08-12 ruling):** runs carrying a `param_hash`,
  deduplicated by (param_hash, code_stamp), with unstamped runs counting singly — §2 above is
  why sameness cannot be assumed where the code was never stamped. At E1's scoring time the
  ledger holds trials 54, 55, 56 + the E1 run itself; `MIN_TRIALS = 50` floors the deflation.
- **DSR frequency:** computed entirely in per-session terms — Sharpe and skew/kurtosis from the
  same daily return series. The work order's words are "the trade distribution's skew/kurtosis";
  the Bailey–López de Prado expression is defined on the return series the Sharpe is measured
  on, and mixing a daily Sharpe with per-trade moments would put two frequencies in one formula.

A full-window pass whose OOS bootstrap-median loses to the benchmark is demoted to unproven with
the reason on the record — §2.5(a) makes the OOS cut part of the definition, not decoration.
Writes `stats.bars_25`; wired into `backtest.yml` beside the audit; `dd_bar` joined the dispatch
inputs so E3 cells can declare their −0.34 bar at dispatch time.

## 5 · E1 — the Micron question

`e1` is a preset: `a1v` with `MU.US` excluded from the sleeve's reach, derived from `a1v` the
way `a1v` is derived from `a1`, so the exclusion is the only difference (pinned by test). The
skip sits before every entry door; MU still ranks; the slots stay open; the capital the sleeve
does not ask for follows the chassis into the park. The conformance table declares the exclusion
and counts a leak as a violation. This is the honest form of §2.5(b)'s jackknife — the world
re-run without the name, not a subtraction that keeps the compounding the name financed.

### The result — run 57, and the Micron question is answered

| | Return | CAGR | Max DD | Trades |
|---|---|---|---|---|
| A1V, with MU (53) | +295.25% | 16.64% | −30.23% | 97 |
| **E1 — A1V ex-MU (57)** | **+159.31%** | **11.24%** | −32.70% | 130 |
| VOO, matched window | +259.82% | 15.45% | −33.99% | — |
| 90/10 VOO/cash, matched window | +221.17% | — | — | — |

**Strip Micron and the overlay does not merely lose its edge — it costs money against doing
nothing.** E1 lands 63 points *below* the 90/10 counterfactual and 100 points below VOO. The
sleeve was not starved without MU: it traded MORE (130 against 97 — the freed slot and capital
were actively redeployed into other names), and the redeployment subtracted. One name was worth
136 points of total return; §7i's "the result is one position" is now priced exactly.

Conformance passed (the exclusion is declared on the Entry clause, zero leaks). Both new steps
ran clean on their first live data. The **§2.5 verdict: UNPROVEN**, failing every evidence
clause — the edge does not survive removing the top 3 winners; bootstrap-median CAGR 11.01%
against the benchmark's 15.45%; deflated Sharpe 0.722 against the 0.95 bar (trials logged: 4 —
runs 54–57 per the forward-only rule).

The capture audit puts a denominator under nine years of this machinery for the first time:

- **The net: 30 of 2,181 pushes entered — 1.4%.** (854 more races were unresolved when the
  window closed.) The A1 machinery is not a push-catcher that keeps losing; it is a machine that
  almost never meets the population it was built for.
- **The ride: 19.75% of the dollars kept, median share −0.7%.** Even when it caught a push, the
  median catch made nothing — clipped, not ridden.
- **The junk: 97 of 127 positions (76.4%) overlapped no push at all and lost $69,391 between
  them.**

One honest nuance for the record: the **OOS cut (Aug-2025 on) beat both yardsticks without MU** —
+40.56% against VOO's +25.43% and the counterfactual's +22.72%. The last year of the machinery,
ex-MU, worked. That is one year, on 130 trades' tail, and it is exactly the kind of stretch §2.5
exists to keep from being called a finding; it is recorded, not believed.

**Per the work order's pre-registered read:** the overlay is unproven at current breadth. E3
proceeds regardless — breadth is the prescribed fix either way — but sizing ambition
recalibrates downward. The capital rule (the park) remains the only proven thing in the
programme, and E1 makes that sharper: A1V's entire margin over the chassis was one name.

## 6 · The audit's first catch beyond E1: the census holds a test symbol

The top "missed push" of the decade was **ZVZZT.US at +2,555% in one day** — NASDAQ's test
symbol, sitting in `universe` as an active stock and offered to every backtest's ranking since
first light (never traded — luck, not protection). The same table surfaced two duplicated
series: LAZR_old.US / LAZRQ.US (1,660 identical close+volume bars — one series, two symbols) and
TRCH.US / MMAT.US (1,213 of 1,229 overlapping daily returns identical — the pre-merger line and
the merged history; runs 43/49/50 traded the pair five times, the same move under two names).

Migration `045_a_test_symbol_is_not_a_stock.sql` excludes all three, with the measurements in
the file. Run 57 held none of them, so its simulation stands; its audit denominator carries ~4
phantom pushes (two ZVZZT, one per duplicate pair), which moves the net from 1.38% to 1.38% —
noted for exactness, immaterial in effect. Every later run inherits the clean census.

## 7 · The ruling, and the first honest A2 (run 58)

Zak ruled the two open clauses the same evening (amendment recorded in
[`wo-e-series-2026-08-12.md`](wo-e-series-2026-08-12.md)): **slot ordering = first-come-first-
served by breakout date, ADDV tiebreak** (`slot_order='breakout_fcfs'`, the null ordering — the
thesis says breadth and holding are the constraint, not selectivity, so the queue now says the
same thing), and **the MCN ≥ 70 floor off for A2** (`entry_mcn_floor=False` — E3's door is
breakout + M2 and names no score; run 56 had refused 752,675 candidate-days below a floor the
spec never asked for). `trend_vol` (calmest trend first) is the one pre-registered alternative
arm, preset `a2o`. The ordering is now a declared conformance clause on every run, the law
included, and an unrecognized ordering halts rather than falling back to MCN.

**Run 58 is the first A2 that is actually A2** — first to pass conformance, first with a green
heartbeat, provably a new experiment under the completed P1 stamp.

| | Run 58 | E3 pass bar | VOO | 90/10 |
|---|---|---|---|---|
| Return | +104.11% | — | +260.71% | +221.88% |
| CAGR | 8.30% | > benchmark | 15.44% | — |
| Max DD (realized) | **−36.47%** | ≥ −34% | −33.99% | — |
| Bootstrap DD, 5th pct | **−62.18%** | ≥ −34% | — | — |
| Trades / win / hold | 1,648 / 43.9% / 15.0d | — | — | — |
| Momentum exposure | 59.6% | — | — | — |
| Conformance | **pass** | pass | — | — |

**Verdict: DEAD**, by the arm's own pre-registered kill rule — the bootstrap 5th-percentile
drawdown breaches the declared −34% bar by 28 points, and the realized path already breaches it
unresampled. For completeness it fails everything else too: bootstrap-median CAGR 8.01% against
15.44%, DSR 0.538, ex-top-3 +77.3% against the benchmark's +260.7%, and the OOS cut loses
(+13.86% against +25.74%).

**The queue defect is gone, and the audit proves it.** The net went from E1's 1.4% to **13.7%**
(298 of 2,178 pushes entered — ten times A1's machinery), the ride from 19.8% to **35.1% of
dollars kept** (median 24.3%), and every name run 56 could never reach was bought: NVDA 38
trades, MSFT 37, AAPL 21, GOOGL 16, AMZN 15, NFLX 15, AVGO 11, LLY 10 — and MU +$39k caught
mechanically (best trade +178.8%), no conviction involved.

**What the fix exposed is the next defect in line: the exits clip what the queue now admits.**
The mega-caps were bought constantly and held 10–22 days for pocket change — on a low-ATR
mega-cap, 3×ATR(20) is an ordinary week's pullback, and the Chandelier only takes over at +1R,
which a tight-ATR name rarely reaches before the stop fires. Meanwhile 1,184 of 1,648 positions
(71.8%) overlapped no push at all and lost **$384,613** between them — the junk book at 60%
exposure is what drives a −62% resampled tail. The one clean catch shows the ceiling: ASTS's
July-2024 push was ridden for 74.7% of the hold-to-end move. The machinery can hold a trend; it
mostly never gets to.

Per the kill rule there is no re-tuning beyond the pre-registered arms. What remains sanctioned
on this axis: the E3 sensitivity ladder (trail {5×, 10×}, risk {0.25%, 1.0%}, positions
{20, 50}, entry {252d → ATH}, trims, ordering {fcfs → trend_vol}) — each cell a logged trial —
then E2, E4, and E-final on survivors. Whether to spend ladder runs on a centre that died on
risk is Zak's call, not a default.

## State after this session

| | |
|---|---|
| Done | P1 (both halves) · §2.1 · §2.2 · §2.5 bars **applied** · capture audit (standing, automatic) · A2 conformance clause found + fixed · census cleaned (045) · **E1 answered: unproven — the overlay ex-MU loses to the chassis** · **E3 unblocked by ruling and its centre measured: run 58, DEAD on the drawdown bar** |
| Demoted | E0 — Zak ruled: stop re-scoring old runs |
| Open, Zak's | Spend the pre-registered E3 ladder cells on a centre that died on risk, or move to E2 / E4 |
| Not started | E2 (C1 falsification) · E4 (VOO+SPMO blend) · E-final (bear gate) |

Follow-ups worth a line: the E-series trial ledger now deduplicates on (param_hash, code_stamp),
so a re-run of an unchanged experiment no longer inflates the §2.5 deflation count; the capture
audit's `worst_names` / `missed_top` tables are the standing place to look for census defects —
045's three exclusions all came from there; and run 58's exit census (mega-caps held 10–22 days
under a 3×ATR stop that never reaches the Chandelier) is the measured starting point if the
trail/stop ladder cells are ever run.
