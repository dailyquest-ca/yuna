# WO-A5 — building out the concentrated arm, pre-registered

**Written before the cells below are run.** That is the whole point of the file: `lg8_semi_trail`
came out of a twenty-two-cell search and reached §2.5 `proven`, and the next honest step is to
try to break it under rules fixed in advance. Anything decided after a number is known is not a
test.

Status of the thing being tested — run 204, the authoritative grid:

| | CAGR | realized DD | bootstrap median DD | bootstrap p5 DD | entries/yr | DSR | §2.5 |
|---|---:|---:|---:|---:|---:|---:|---|
| `lg8_semi_trail` | 31.75% | -31.2% | -31.0% | -44.4% | 16.7 | 0.979 | proven |
| SPMO alone (run 87) | 21.12% | -31.0% | **-30.95%** | **-42.86%** | 0 | — | benchmark |

---

## 1 · The drawdown bar — RULING REQUIRED (Zak, §4.5)

**A4 declared no drawdown bar.** E3's centre was killed by exactly that clause at -34%; this arm
never faced it, so `proven` above means proven on return, robustness and the OOS cut, and silent
on risk. A bar must exist before the arm goes further, and it must be set now rather than after
the ladder below runs.

**Proposed, and derivable rather than chosen:** the sleeve's entire justification (§2.4.1) is
beating the park. A sleeve that risks more than the park has to pay for the difference. So the
bar is **the park's own drawdown distribution, measured the same way**:

- **Clause 1** — bootstrap median drawdown must not exceed SPMO's **-30.95%**
- **Clause 2** — bootstrap 5th-percentile drawdown must not exceed SPMO's **-42.86%**

Measured today, `lg8_semi_trail` **fails both, marginally**: -31.0% against -30.95% (5 basis
points) and -44.4% against -42.86% (1.5 points). Stated plainly because it is the uncomfortable
reading and the one that matters: **the champion buys 10.6 points of CAGR for slightly more tail
risk than the park, and whether that trade is acceptable is a risk-posture call — Zak's alone.**

Three ways it could be ruled, all legitimate, none of them mine to pick:

1. **As proposed** — the sleeve must not risk more than the park. The champion fails and the arm
   needs a tighter trail before it goes further.
2. **With a stated tolerance** — e.g. the sleeve may exceed the park's tail by N points because
   it returns 10.6 more. If so, N is a number Zak sets, in the plan, before the ladder runs.
3. **On a different statistic entirely** — e.g. time-to-recovery, or drawdown in CAD after FX.

Until this is ruled, **nothing below changes what the arm is allowed to be called.** The ladder
runs regardless — it is about whether the result is real, not whether the risk is acceptable —
but no cell may be described as passing a bar that does not exist.

---

## 2 · The realism gaps, and which are closable today

### 2.1 The stop cannot be filled at the stop (CLOSABLE — the data is there)

`concentrated.simulate` tests §3.2's trail on the **close** and fills at the **next close**,
because the tape was loaded without a range. `prices` carries `high` and `low` on all 8,858,709
rows, and `capture_audit.load_tape(with_range=True)` already returns them.

This is not a small approximation and it does **not** obviously flatter:

- filling at the stop rather than a session later is **better** on a name that keeps falling
- but a real stop also fires on an **intraday spike the close recovered from** — a shakeout the
  close-based test never sees — which is **worse**, and is the classic reason mechanical stops
  underperform their backtest
- and a **gap through the stop** fills at the open, not the stop

All three are now testable. Pre-registered: the ladder below runs on close-based fills (as
measured) **and** on intraday fills, and both are reported. If the champion's CAGR falls by more
than 5 points on intraday fills, the close-based number was an artefact of the fill model and the
intraday number is the one that counts.

### 2.2 Earnings are invisible to this arm (CLOSABLE, lower priority)

The concentrated book holds through every earnings date with an 8% initial stop. §3.2 legislates
a hold-through rule requiring a +8% cushion for a reason: an earnings gap does not respect a
stop. The store has an earnings calendar. Not in this round; named so it is not forgotten.

### 2.3 The window has one bear market and cannot be extended (NOT CLOSABLE — needs a backfill)

`prices` runs from **2003-08-18**, but only for SPY and two other ETFs. The **stock universe
begins 2016-08**: June 2016 has 3 tickers, June 2017 has 3,620. So the tape covers 2018, 2020,
2022 — and no 2008, and critically **no 2009**, which is the momentum crash Barroso &
Santa-Clara built their governor for and the single most informative regime this strategy could
be shown.

Closing it means a bulk EODHD pull of ~3,000 names x 13 years. That is a real API cost and a real
ingest job, and it is **the highest-value item on this list by a distance** — a momentum result
that has never seen a momentum crash is a result with a known blind spot, however good its
bootstrap looks. Scoped here, not run: it needs Zak's go-ahead on cost before anything fetches.

---

## 3 · The robustness ladder — DECLARED BEFORE RUNNING

A champion picked from twenty-two cells must be a **plateau**, not a spike. Every axis moves one
step either side of `lg8_semi_trail`; the arm is robust if the surface around it is smooth and
fitted if it is not.

| axis | champion | one step down | one step up |
|---|---|---|---|
| names held | 8 | 6 | 10 |
| pool (top-K by ADDV) | 500 | 250 | 750 |
| trail band | 10% | 8% | 12% |
| arm threshold | +15% | +12% | +18% |
| initial stop | 8% | 6% | 10% |
| euphoria band | 5% | 4% | 6% |
| clock | semi-annual | quarterly | annual |

**Pre-registered reading, so it cannot be rationalised afterwards:**

- **robust** — every neighbour within **5 CAGR points** of the champion, and no neighbour's
  bootstrap median drawdown more than **5 points** worse
- **fitted** — any single-step neighbour losing more than 10 CAGR points
- **anything between** — reported as-is, with the axis named; a strategy sensitive on ONE axis is
  a strategy with one parameter that needs a source in the plan, not a dead one

Note what this ladder is not: it is not a search for a better cell. If a neighbour beats the
champion, that is **evidence about the surface**, not a new champion — promoting it would be
re-running the same twenty-two-cell selection with extra steps, and the deflated Sharpe already
prices that.

### 3.1 Cost sensitivity

§2.2's spread curve at **1x, 2x and 4x**. Momentum at 16.7 entries a year is not cost-fragile in
principle; "in principle" is not a measurement. Pre-registered: if 2x costs remove more than 3
CAGR points, the arm is thinner than its trade count suggests.

---

## 4 · What is deliberately NOT in this work order

- **No new cell families.** The grid is closed. Adding an axis after seeing the answer is the
  search the deflation exists to punish, and 63 trials is already a wide net.
- **No parameter tuning.** Every constant in the champion traces to §3.2's own text. If the
  ladder shows a neighbour is better, the finding is "the surface tilts that way", and any change
  to the law is a plan edit first.
- **No production role.** §2.4.2's concentration review has not run on this arm, the drawdown bar
  above is unruled, and nothing in this repo places an order regardless.

---

## 5 · Order of operations

1. This file is committed **before** any cell in §3 runs. ✅
2. The ladder runs on close-based fills — the model as measured.
3. Intraday fills are implemented and the ladder re-runs; both are reported.
4. Zak rules the drawdown bar (§1). Only then does any cell get called a pass or a fail on risk.
5. The backfill (§2.3) is costed and put to Zak as its own decision.


---

## Amendment, same day — the third fill model, and how it came to be proposed

**Disclosure first: this was written after the intraday cells came back 5.9 CAGR points below the
close-based ones.** That is exactly the sequence §4 of this file warns about, so the argument is
set out on its own terms and the reading is fixed before the cells run.

`stop_fill` models a **resting GTC stop** at the broker. That is one execution path. The other is
a decision taken at the close and placed for the next morning — and this repo's own law uses
both: §3.2 rests stop-limit orders at the broker for entries, and its hair-trigger "exits next
morning"; §5.1 has Zak reading a brief and placing every order in the morning. Which path this
arm would actually run under is an **operational question about how Zak trades**, not a modelling
preference to be settled by whichever number is larger.

The three models, all reported, none replacing another:

| model | trigger | fill | who runs it this way |
|---|---|---|---|
| close-based | close breaks the trail | next close | nobody — it is the tape's limitation |
| intraday | low touches the resting stop | the stop, or the open on a gap | a GTC stop left at the broker |
| next-open | close breaks the trail | next session's open | a person reviewing at night, per §5.1 |

**Pre-registered reading:** the close-based model is a modelling artefact in all three cases and
is reported only as the historical baseline. The arm's real number is **whichever of intraday or
next-open matches the execution path Zak rules for it** — and if he has not ruled, the arm is
described by the WORSE of the two, because an unruled execution path is not an assumption the
result gets to make in its own favour.

**Measured, corrected ladder (runs 229-248):**

| cell | CAGR | bootstrap median DD | DSR | §2.5 |
|---|---:|---:|---:|---|
| `lg8_semi_trail` (close-based) | 31.75% | -31.0% | 0.962 | proven |
| `lg8_trail_intraday` (resting stop) | **25.89%** | -30.6% | 0.922 | **unproven** |

The close-based number falls 5.9 points under a resting stop — past this file's own 5-point
threshold — so **under a GTC stop the champion is not proven, and 25.89% is the number that
counts.** The drawdown barely moves (-30.6% against -31.0%), which locates the whole cost in the
shakeout: a real stop fires on intraday spikes the close recovered from, and gives up the rest of
the move each time.

## Ladder result: the champion is a plateau

Every single-step neighbour lands within **5 CAGR points** of the champion, against a criterion
fixed before any of them ran. Thirteen of the twenty cells reach `proven` in their own right.

| axis | down | up | widest move |
|---|---|---|---|
| names (6 / 10) | 33.36% | 29.94% | ±1.8 |
| pool (250 / 750) | 27.43% | 27.78% | **-4.3** |
| clock (quarterly / annual) | 32.95% | 30.85% | ±1.2 |
| trail band (8% / 12%) | 30.79% | 32.14% | ±1.4 |
| arm (+12% / +18%) | 30.74% | 30.97% | ±1.0 |
| initial stop (6% / 10%) | 31.11% | 30.69% | ±1.1 |
| euphoria (4% / 6%) | 31.29% | 31.37% | ±0.5 |
| **costs 2x / 4x** | 31.19% | 30.09% | **-0.6 / -1.7** |

**Robust.** The pool is the only axis with real curvature and the champion sits at the top of it —
which is a mild warning in itself, since 500 was chosen from the original grid rather than
derived. Cost sensitivity passes comfortably: 2x costs remove 0.6 points against a pre-registered
3-point threshold, so 16.7 entries a year is genuinely not cost-fragile.

Two defects were found in the first ladder pass and are recorded in the session log: the intraday
stop was tested against the entry session's own bar (a look-ahead running backwards, worth 0.7 of
the 6.5 points first reported), and the stop set at entry ignored the cell's own band, so
`lad_init10` varied nothing and returned byte-identical to the champion.


---

## The three fill models, measured (runs 229-232, 249-250)

| model | execution path | CAGR | boot median DD | boot p5 DD | DSR | §2.5 |
|---|---|---:|---:|---:|---:|---|
| close-based | none — the tape's limitation | 31.75% | -30.96% | -44.36% | 0.962 | (baseline) |
| **intraday** | a GTC stop resting at the broker | **25.89%** | -30.56% | -44.28% | 0.922 | **unproven** |
| **next-open** | reviewed at night, market-on-open | **32.07%** | **-30.35%** | **-43.69%** | 0.965 | **proven** |

**The spread between the two real paths is 6.2 CAGR points on identical selection rules.** Nothing
about which stocks to own changes between them. The entire difference is whether the exit order
sits at the broker overnight or is placed the next morning — and the resting order loses because
it fires on intraday spikes the close recovered from, surrendering the rest of each move.

Per the reading fixed above, **absent a ruling the arm is described by the worse of the two:
25.89%, unproven.**

### This updates §1's drawdown bar

Against the park's own distribution (SPMO: median -30.95%, p5 -42.86%):

| | clause 1 (median) | clause 2 (p5) |
|---|---|---|
| intraday | **passes** (-30.56%) | fails (-44.28%) |
| next-open | **passes** (-30.35%) | fails (-43.69%) |

Both paths now **pass the median clause** — the champion's typical drawdown is no worse than the
park's, and on the next-open path it is slightly better. Both still **fail the tail clause** by
about a point. So the §1 ruling narrows to one question: **is roughly one extra point of
5th-percentile drawdown an acceptable price for 10.9 points of CAGR over the park?** That is the
whole risk-posture call, and it is Zak's.

### RULING REQUIRED (Zak, §4.5) — the execution path

Not a modelling choice. §3.2 legislates both paths and §5.1 has Zak placing every order from a
morning brief, which points at next-open; but a resting stop is what protects a book on a day
nobody looks at it, which is the whole reason §3.2 rests them at the broker for entries. The
honest framing is that **the morning-order path earns 6.2 points a year by accepting that a
crash between one close and the next open is unprotected.**


---

## RULED (Zak, 2026-08-13)

1. **Drawdown bar — waived.** *"Of course the drawdown risk is OK. Things drawdown, I don't mind
   drawdowns. I don't need this money for the next 20 years! I'm just looking for highest
   compounding right now."* §1's park-parity clauses do not bind this arm. Recorded with what is
   being accepted: a bootstrap 5th-percentile drawdown of **-43.7%**, and the honest limit on that
   estimate — it is resampled from a window containing no 2009, so the true tail is very likely
   worse than the number just accepted.
2. **Execution path — next-open.** *"That's how I'd like to operate anyways."* The arm's number is
   `lg8_trail_nextopen`. The accepted cost is stated once: a crash between one close and the next
   open is unprotected, which is exactly what a resting stop covers.
3. **Backfill — declined for now.** V1 first, more data in V2.

**With those rulings, the arm's measured result is 32.07% CAGR / -31.3% realized drawdown /
DSR 0.965 / §2.5 PROVEN, at 16.7 new names a year.**

---

## The finding the ladder did not look for: the book is one bet

The ladder moved seven axes and found a plateau. It never asked what the eight names *were*.

**The July 2026 book, bought 2026-07-01:** MU, SNDK, WDC, STX, LITE, CIEN, BE, AXTI. That is the
memory / storage / optical complex — eight names and one trade. Every one of them stopped out
between 2026-07-06 and 2026-07-08, all at a loss, in two to five sessions.

Run against §2.2's own formula on the 126 sessions before entry:

| | |
|---|---|
| average pairwise correlation | **0.477** |
| **effective bets** (1 / Σ wᵢwⱼρᵢⱼ) | **1.84** |

**The plan flags any book below 4 effective bets.** This one was 1.84 — under half the warning
line — and the January 2026 book was half gold miners (AU, B, HL, KGC). This is not bad luck; it
is what 12-1 momentum over a 500-name pool *does*, because the names running hardest at any moment
are usually running for the same reason.

Nothing in the concentrated arm applies §3.3's theme cap or §2.2's effective-bets check. The
backtest's headline is therefore **an 8-name book that is frequently a 2-bet book**, and the
32.07% is earned partly by taking that concentration. It is the single largest untested risk in
the arm and it is squarely inside the plan's existing law rather than a new idea.

### Consequence for a V1, stated plainly

The strategy is currently **100% SPMO** and has been since 2026-07-09, and its next scheduled
decision is **2027-01-01** — a stop-out wave parks the book until the next rebalance, up to six
months. `lad_quarter` (32.95%, DSR 0.952, `proven`) is statistically indistinguishable from the
champion and offers four decision points a year instead of two. Per §3 that is **evidence the
clock axis is flat, not a new champion** — which means the clock can be chosen on operational
grounds without costing measured return.
