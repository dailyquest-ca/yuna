# Handoff — E-series, resume point

Branch `claude/backtest-spec-review-8fd7d7`. Everything is committed and pushed. Read
[`wo-e-series-2026-08-12.md`](wo-e-series-2026-08-12.md) (the work order) and
[`arms-2026-08-12.md`](arms-2026-08-12.md) (every run and what each one actually measured).

## One decision blocks everything

**A2 needs a slot-ordering rule, and it is Zak's to make.**

The book holds 30. On most days in a bull market, more than 30 names make a new 252-session high.
Something has to decide who gets the slots, and today that something is MCN — inherited from §3.2,
never specified by E3. MCN rewards explosive short-horizon momentum, so a mega-cap grinding out
+30%/yr never outranks a small-cap that ran 40% in six weeks.

The consequence is measured, not theoretical: across runs 54, 55 and 56, **MSFT, GOOGL, AMZN, AVGO,
LLY and NFLX were never bought once in nine years**, and NVDA was held twice for a loss. All are
`in_l0`, active, 2,244 bars, eligible every single day.

Defensible orderings, each selecting a completely different book:

- first come, first served by breakout date — truest to "buy the breakout when it happens"
- longest continuous time above the 200-day
- furthest above the 200-day
- lowest volatility of the trend
- market cap

E3 names none of them. Picking one silently would be inventing the strategy rather than testing it.

## Standing rule to adopt

**No arm's number is believed until a capture audit runs.** Runs 54, 55 and 56 all passed their
accounting invariants, produced plausible CAGRs, and were each measuring something other than the
specified arm. Four defects — a breakeven stop hidden in the confirmation machine, an entry door
that was never gated, A1's candidate pool, A1's candidate order — and **not one was visible in a
summary statistic.** The question that found all of them was "did it buy NVIDIA".

The audit has three parts, and only the first exists so far (as ad-hoc SQL, in arms-2026-08-12.md):

1. **The net** — enumerate every large momentum push in the window mechanically (name-months
   running +50%+ off a 252-high). What fraction did the arm enter at all?
2. **The ride** — for each one caught, what share of the move was kept? Entered near the start and
   held, or clipped after three days?
3. **The junk** — of everything held, what share was neither a push nor caught one?

Build it as a reusable script so every arm gets it automatically. CAGR alone cannot distinguish
"caught 70% of the pushes and kept 40% of each" from "caught 10% and got lucky twice".

## State

| | |
|---|---|
| Done | P1 · §2.1 banded true-up · §2.2 cost curve · §2.5 stat bars (built, unapplied) |
| Demoted | E0 — Zak ruled: stop re-scoring old runs |
| **Blocked** | **E3 / A2 — needs the ordering rule** |
| Not started | E1 (Micron) · E2 (C1 falsification) · E4 (VOO+SPMO blend) · E-final (bear gate) |

**A2 is untested.** Runs 54/55/56 measured a hybrid with a breakeven stop, a hybrid with A1's
candidate pool, and a hybrid with A1's candidate order — in that order.

Two open items unrelated to the blocker: conformance reports `false` on A2 runs and the failing
clause has not been identified (narrowed to coverage on two earnings clauses); and the §2.5 bars
are built but have never been applied to a run.

## The result that survives all of this

**A1V: +295.25%, CAGR 16.64%, max DD −30.23%** against VOO's +260.99% / 15.44% / −33.99%, and
against a 90/10 VOO/cash counterfactual of +222.10%. The capital rule — 10% cash, idle capital in
the index — is the one thing proven today, and it does not depend on any strategy finding. The
sleeve's contribution over that counterfactual is 73 points across nine years, and it inherits
A1's single-name dependency: 71.4% of A1's profit is one Micron trade. E1 decides that.

---

## Picked up 2026-08-13 — what the next reader should know instead

Continuation on branch `claude/pickup-previous-session-s3wduf` (this branch supersedes the resume
point above; the session record is [`session-2026-08-13.md`](session-2026-08-13.md)).

**The "two earnings clauses" narrowing above was wrong.** Runs 54–56's conformance failure was
never about coverage — M4 read 0.982 and the blackout 0.948 on run 56, both fine. It was the
**Entry clause**: every A2 entry landed in the `reentries` counter, and the violation waiver knew
only the two non-base doors earlier variants had declared (X1's window, C1's screen), so the
arm's own entry rule was counted as 1,014 undeclared violations of a re-entry clause. Fixed: the
new-high door has its own counter and declaration, and an undeclared new-high entry still fails.

Also found on the way: **runs 54, 55 and 56 share param_hash `dd8735ff2ac4dace`** while measuring
three different engines. P1's hash covered the params and not the code, so the ledger called
three experiments the same experiment. `code_stamp()` now folds the driver and law module into
the digest.

Disposed since the state table above: the capture audit is `src/capture_audit.py` (all three
parts, unit-tested, wired into every backtest run), the §2.5 bars have their driver in
`src/finding.py` (both wired as automatic steps in `backtest.yml`), and **E1 ran and answered:
run 57, A1V ex-MU, +159.31% against the counterfactual's +221.17% — the overlay without its one
name loses to doing nothing, verdict UNPROVEN on every §2.5 clause.** Its capture audit also
caught NASDAQ's test symbol and two duplicated series living in the census (migration 045).

**The ordering decision was then ruled by Zak the same day** — FCFS by breakout date, ADDV
tiebreak, MCN floor off for A2 (amendment in the work order) — and the E3 centre spec ran as
run 58, the first A2 that is actually A2: capture fixed (NVDA bought 38 times, every excluded
mega-cap traded), **verdict DEAD on the pre-registered −34% drawdown bar** (bootstrap 5th
percentile −62.2%, realized −36.5%). The blocker above is resolved; what remains open is whether
to spend the pre-registered E3 ladder cells on a centre that died on risk, or move to E2/E4 —
see `session-2026-08-13.md` §7.


---

## Second handoff, same day — read this before trusting any A4 number

Everything above stands. What follows supersedes `session-2026-08-13.md` §12 entirely.

The concentrated arm (WO-A4) had **three defects in its simulation and two in its scoring**, all
found by querying the trade ledger rather than reading its summaries, and all recorded with the
measurements that produced them in `session-2026-08-13.md` §13. The largest by far:

> `build_grid` took its sessions from the tape's union of dates, so US market holidays — on which
> ~26 junk listings print and VOO does not — were sessions. The first session of each half-year
> landed on New Year's Day four times over nine years. On such a day no name clears the $5 floor,
> the rank is empty, the book sells everything and buys nothing, and **the account sits in SPMO
> until July.** Every A4 cell was roughly half-parked, which is why every one of them pinned to
> SPMO's own -30.95% drawdown on 2020-03-23 — a session on which the concentrated book held no
> stocks at all.

The calendar is now the benchmark's own bars, and `build_grid` refuses to build without one.
`stale_skips` and `empty_rebalances` are recorded on every run so the shape cannot hide again.

Migrations **047** and **048** excluded 36 duplicate listings — the same company under two
symbols, closes agreeing to the cent, which let a twelve-name book hold Overstock in four slots
and Block in two. 048 exists because 047 signed on closes over the dates each line *has*, and so
missed exactly the pairs whose coverage differed. Read both headers before adding a third; the
failure modes are written down.

A4 runs now carry a `param_hash`, so the grid's twenty-two cells enter the trial ledger and pay
for their own width in the deflated Sharpe. They did not before, which meant a champion picked
out of twenty-two was being scored as though the search had not happened.

**Runs 95-164 are all superseded.** Do not quote them.
